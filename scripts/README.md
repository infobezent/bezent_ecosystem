# scripts/

Repository-level automation scripts (release, DB bootstrap helpers, CI
utilities, etc.).

Empty in Phase 0 — no repo-level scripts are needed yet. Add scripts here
only when a concrete, recurring need arises.
