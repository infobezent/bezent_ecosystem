# API Documentation

- **[STANDARDS.md](STANDARDS.md)** — the source of truth for API design:
  versioning, pagination/filtering/sorting, validation, error shape, auth,
  tenant scoping, and the current endpoint list.

Endpoint-specific documentation (request/response schemas per resource) is
added here as real endpoints are built. Phase 0 has one endpoint,
documented in STANDARDS.md: `GET /api/v1/health`.
